export class AccountDetails {
  constructor(public id: any, public AccountNumber: string, public AccountType: string, public Balance: number, public DateCreated: Date, public IsActive: boolean) { }
}
